# Documentation

- `research/`: current method terminology and the formal Stage 2 protocol.
- `legacy/`: documentation for superseded methods and experiments.
- `assets/`: versioned documentation assets.

Normal plots belong in `predictions/`. One-off A0, R, M0/D0 reports and their
protocols remain with the corresponding run under `tests/audits/`.
