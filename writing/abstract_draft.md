# Abstract Draft

## English

Ground vehicles on rough terrain need paths that reach a goal while avoiding impassable ground, rollover-prone terrain, and overly sharp turns. Learning-based generators are fast, but directly predicting all path coordinates leaves start and goal positions and headings to learning, while imitation alone does not explicitly optimize terrain-dependent physical requirements. We propose **Path MeanFlow** with two main designs. First, a boundary-aligned trajectory representation analytically embeds the start and goal positions and headings in a spline decoder, so the network predicts only the route between them. Second, the generator learns long-range route structure from expert paths before undergoing **privileged multi-objective terrain adaptation (PMTA)**, which coordinates gradients for planning-region violation, rollover stability, and curvature. During learning, complete terrain is used only by the stability objective. To condition generation on current terrain evidence, a mask marks observed cells that may support the vehicle center; unknown and forbidden cells are unavailable. At deployment, Path MeanFlow uses only this masked observation and generates one path in a single forward pass, without online physical-cost evaluation or trajectory optimization. [Results pending frozen experiments: on an unseen-terrain simulation benchmark, report Feasible@1 against the best evaluated partial-information baseline, the change from expert-route learning alone, route-distribution change, and p95 latency.]

## 中文

崎岖地形上的无人地面车辆需要一条能够到达目标，同时避开不可通行地形、容易导致倾覆的区域和过急转弯的全局路径。学习式生成器能够快速产生路径，但直接预测全部路径坐标会使起终点位姿依赖网络学习；仅模仿专家路线，也不会显式优化具体地形上的多项物理要求。为此，我们提出具有两项核心设计的 **Path MeanFlow**。第一，边界对齐轨迹表征将给定的起终点位姿解析写入样条解码器，使网络只需生成两端之间的路线形状。第二，生成器先从专家路径中学习长距离路线结构，再通过 **特权多目标地形适应**（privileged multi-objective terrain adaptation，PMTA），协调路径越出当前规划区域、车辆倾覆风险和过大曲率三项目标的梯度；在学习过程中，完整地形仅用于稳定性目标。为了使路径响应当前可用的地形信息，显式掩码标识已经观测且允许车辆中心进入的单元格，未知区域和已知禁入区域均不可用。部署时，Path MeanFlow 只使用带掩码的地形观测，通过一次网络前向生成一条路径，无需在线计算物理代价或执行轨迹优化。[结果句等待冻结实验：在未见地形仿真基准上，报告相对实际评测中表现最好的部分信息基线的 Feasible@1、相对专家路线学习阶段的变化、路线分布变化和 p95 延迟。]
