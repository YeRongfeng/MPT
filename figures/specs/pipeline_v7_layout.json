{
  "title": "Method overview v7 - publication-compatible candidate",
  "canvas": {"width": 1600, "height": 760},
  "composition": {
    "top": "model provenance: demonstrations -> route learning -> physical adaptation -> trained generator",
    "bottom": "deployment spine: observation -> generator -> task-aligned free coordinates -> analytic decoder -> path",
    "shared_anchor": "the adapted generator and deployed generator share the same teal visual identity; no cross-figure weight wire",
    "training_nodes": ["expert demonstrations", "route learning", "physical adaptation", "adapted generator"]
  },
  "visual_rules": [
    "no explicit backpropagation loop in the overview",
    "only two contribution badges",
    "training-only complete terrain terminates inside physical adaptation",
    "deployment path is the strongest visual line",
    "input and output thumbnails use a muted repository terrain map rather than generic scenery",
    "no long connector crosses the training and deployment bands",
    "complete terrain connects only to the rollover objective",
    "ASCII-only rendered labels for conversion compatibility",
    "minimum body text remains readable in a full-width IEEE figure"
  ]
}
