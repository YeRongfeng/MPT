# Visualization utilities

This directory contains project plotting and result-inspection scripts. Run
them from the repository root with module syntax, for example:

```bash
python -m visualization.dataset_visualize
```

Normal prediction and visualization artifacts are stored under
`predictions/`, not `tests/audits/`.
